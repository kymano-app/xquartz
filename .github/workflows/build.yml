name: Build
on:
  push:
    branches:
      - 'master'
  pull_request:
  release:
    types: [created]

jobs:
  build:
    name: Build
    runs-on: macos-11
    strategy:
      matrix:
        arch: [x86_64]
        platform: [macos]
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          submodules: recursive

      - name: Install dependencies
        run: |
          wget https://xorg.freedesktop.org/archive/individual/proto/xcb-proto-1.14.1.tar.xz
          tar -xzf xcb-proto-1.14.1.tar.xz
          cd xcb-proto-1.14.1
          sed -i '' -e "5s/^//p; 5s/^.*/PYTHON_PREFIX=@PYTHON_PREFIX@/" xcb-proto.pc.in
          cat xcb-proto.pc.in
          ./configure
          make
          make install
          cd ../
          # sudo mkdir -p /opt/X11/lib/pkgconfig/
          # sudo ln -s /usr/local/lib/pkgconfig/xcb-proto.pc /opt/X11/lib/pkgconfig/xcb-proto.pc
          # cat /opt/X11/lib/pkgconfig/xcb-proto.pc
          export PKG_CONFIG_PATH="/usr/local/lib/pkgconfig"
          ls -lta /usr/local/lib/pkgconfig
          echo $PKG_CONFIG_PATH
          chmod 0777 /usr/local/lib/pkgconfig/xcb-proto.pc
          cat /usr/local/lib/pkgconfig/xcb-proto.pc
          git clone https://github.com/XQuartz/XQuartz.git
          cd XQuartz
          # ./install-or-update-macports.sh
          cat /usr/local/lib/pkgconfig/xcb-proto.pc
          echo "PKG_CONFIG_PATH"
          echo $PKG_CONFIG_PATH

          ./compile.sh